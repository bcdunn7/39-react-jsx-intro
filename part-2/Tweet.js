const Tweet = ({ username, name, date, message}) => (
    <div>
        <span>{ username }</span>
        <span>{ name }</span>
        <span>{ date }</span>
        <span>{ message }</span>
    </div>
)